build:
  docker:
    web: Dockerfile

run:
  web: uvicorn backend.main:app --host 0.0.0.0 --port 8000
  rasa:
    image: rasa/rasa:latest-full
    command:
      - rasa
      - run
      - --enable-api
      - --cors
      - "*"
